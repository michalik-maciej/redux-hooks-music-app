import PageTitle from '../../layout/PageTitle/PageTitle';
import SongsList from '../../features/SongsList/SongsListContainer';

const Songs = () => (
  <section className="page">
    <PageTitle>Songs</PageTitle>
    <SongsList />
  </section>
);

export default Songs;
